MEMORY {
    ZP: start = $E0, size = $10, type = rw;
    RAM: start = $0740, size = $00C0, type = rw;
    CODE: start = $8000, size = $4000, type = ro, file = %O;
}
SEGMENTS {
    ZEROPAGE: load = ZP, type = zp;
    BSS: load = RAM, type = bss;
    CODE: load = CODE, type = ro;
}
